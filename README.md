## Raylib-os

Обертка над https://github.com/raylib-cs/raylib-cs, которая в свою очередь обертка над https://github.com/raysan5/raylib.

Изначально библиотека для создание графических приложений (в т.ч. и игр) с 2D, 3D, графикой, шейдерами и тд. Описание родительской библиотеки: https://www.raylib.com

Целью является предоставить все возможности библиотеки в языке OneScript.

В начальной стадии экпиремента. 

Основы рисования
# Подключение библиотеки
```bsl
КаталогКомпоненты = ОбъединитьПути(ТекущийСценарий().Каталог, "raylibos/bin/Debug/net6.0/raylibos.dll");
ПодключитьВнешнююКомпоненту(КаталогКомпоненты);
Рейлиб = Новый Рейлиб;
```
# Инициализация окна
```bsl
Рейлиб.ИнициализацияОкна("Мое приложение", 800, 600);
```
# Цвета и векторы
```bsl
Белый = Рейлиб.НовыйЦвет(255, 255, 255, 255);
Красный = Рейлиб.НовыйЦвет(255, 0, 0, 255);
Точка = Рейлиб.НовыйВектор(100, 100);
```
# Основной игровой цикл
```bsl
Пока Не Рейлиб.ОкноДолжноЗакрыться() Цикл
    Рейлиб.НачатьОтрисовку();
    Рейлиб.ОчиститьФон(Белый);
    Рейлиб.НарисоватьКруг(Точка.ВекторX(), Точка.ВекторY(), 30, Красный);
    Рейлиб.ЗакончитьОтрисовку();
КонецЦикла;
Рейлиб.ЗакрытьОкно();
```
Ввод с клавиатуры и мыши
# Считывание позиции мыши
```bsl
Позиция = Рейлиб.ПозицияМыши();
X = Рейлиб.ВекторX(Позиция);
Y = Рейлиб.ВекторY(Позиция);
```
# Проверка нажатия клавиш
```bsl
Если Рейлиб.КлавишаНажата(262) Тогда // RIGHT
    X = X + 1;
КонецЕсли;
```
# Проверка нажатия кнопок мыши
```bsl
Если Рейлиб.КнопкаМышиНажата(0) Тогда // LEFT
    // Обработка нажатия левой кнопки
КонецЕсли;
```
2D Камера
# Создание камеры
```bsl
Цель = Рейлиб.НовыйВектор(400, 300);
Смещение = Рейлиб.НовыйВектор(400, 300);
Камера = Рейлиб.НоваяКамера2D(Цель, Смещение, 0, 1);
```
# Использование камеры
```bsl
Рейлиб.НачатьРежим2Д(Камера);
    // Рисование объектов
Рейлиб.ЗакончитьРежим2Д();
```
Доступные методы

Основные функции
- НовыйЦвет(r, g, b, a) - создание цвета
- НовыйВектор(x, y) - создание вектора 2D
- ВекторX(vector) / ВекторY(vector) - получение координат вектора
- ИнициализацияОкна(title, width, height) - создание окна
- ОкноДолжноЗакрыться() - проверка закрытия окна
- НачатьОтрисовку() / ЗакончитьОтрисовку() - начало/конец отрисовки
- ОчиститьФон(color) - очистка фона

Рисование примитивов
- НарисоватьКруг(centerX, centerY, radius, color)
- НарисоватьЭллипс(centerX, centerY, radiusH, radiusV, color)
- НарисоватьПрямоугольник(posX, posY, width, height, color)
- НарисоватьТреугольник(v1, v2, v3, color)
- НарисоватьЛинию(startX, startY, endX, endY, color)
- НарисоватьПолигон(center, sides, radius, rotation, color)

Ввод данных
- КлавишаНажата(keyCode) - проверка нажатия клавиши
- ПозицияМыши() - позиция курсора мыши
- КнопкаМышиНажата(button) - проверка нажатия кнопки мыши
- КурсорСкрыт() - проверка видимости курсора
- ПоказатьКурсор() / СкрытьКурсор() - управление видимостью курсора

2D Камера
- НоваяКамера2D(target, offset, rotation, zoom) - создание камеры
- НачатьРежим2D(camera) / ЗакончитьРежим2D() - режим отрисовки с камерой
- Камера2DЦель(camera) / Камера2DСмещение(camera) / Камера2DВращение(camera) / Камера2DЗум(camera) - получение параметров камеры
- ОбновитьКамеру2D(camera, target, offset, rotation, zoom) - обновление параметров камеры
- ПозицияКолесаМыши() - позиция колеса мыши

3D Графика
- НовыйВектор3(x, y, z) - создание вектора 3D
- НоваяКамера3D(position, target, up, fovy, projection) - создание 3D камеры
- НачатьРежим3D(camera) / ЗакончитьРежим3D() - режим 3D отрисовки
- НарисоватьКуб(position, width, height, length, color) - рисование куба
- НарисоватьКубЛиния(position, width, height, length, color) - рисование каркаса куба

Дополнительные функции
- Случайное(min, max) - генерация случайного числа
- УстановитьЧастотуКадров(fps) - установка частоты кадров
- НарисоватьТекст(text, posX, posY, fontSize, color) - рисование текста

Работа с текстурами
# Загрузка и отрисовка текстуры
```bsl
Текстура = Рейлиб.ЗагрузитьТекстуру("resources/image.png");
Белый = Рейлиб.НовыйЦвет(255, 255, 255, 255);

Пока Не Рейлиб.ОкноДолжноЗакрыться() Цикл
    Рейлиб.НачатьОтрисовку();
    Рейлиб.ОчиститьФон(Белый);
    Рейлиб.НарисоватьТекстуру(Текстура, 100, 100, Белый);
    Рейлиб.ЗакончитьОтрисовку();
КонецЦикла;

Рейлиб.ВыгрузитьТекстуру(Текстура);
Рейлиб.ЗакрытьОкно();
```

# Продвинутая отрисовка (вращение, масштабирование)
```bsl
// Источник - часть текстуры
ИсхПрямоугольник = Рейлиб.НовыйПрямоугольник(0, 0, 64, 64);
// Назначение - прямоугольник на экране
НазПрямоугольник = Рейлиб.НовыйПрямоугольник(400, 225, 128, 128);
// Точка вращения
Начало = Рейлиб.НовыйВектор(64, 64);
Вращение = 45; // градусов

Рейлиб.НарисоватьТекстуруПро(Текстура, ИсхПрямоугольник, НазПрямоугольник, Начало, Вращение, Белый);
```

# Генерация процедурных текстур
```bsl
// Генерация изображения
Изображение = Рейлиб.ГенерироватьГрадиентЛинейный(800, 600, 90, Красный, Синий);
// Загрузка текстуры из изображения
Текстура = Рейлиб.ЗагрузитьТекстуруИзИзображения(Изображение);
// Выгрузка изображения (CPU RAM)
Рейлиб.ВыгрузитьИзображение(Изображение);

// ... использование текстуры ...

Рейлиб.ВыгрузитьТекстуру(Текстура);
```

Методы для работы с текстурами
- ЗагрузитьТекстуру(fileName) - загрузка текстуры из файла
- НарисоватьТекстуру(texture, posX, posY, color) - отрисовка текстуры
- НарисоватьТекстуруПозиция(texture, position, rotation, scale, color) - отрисовка с вращением и масштабом
- НарисоватьТекстуруПрямоугольник(texture, source, position, color) - отрисовка части текстуры
- НарисоватьТекстуруПро(texture, source, dest, origin, rotation, color) - отрисовка с вращением, масштабированием и точкой опоры
- ВыгрузитьТекстуру(texture) - выгрузка текстуры из памяти
- ТекстураШирина(texture) - получение ширины текстуры
- ТекстураВысота(texture) - получение высоты текстуры

Генераторы процедурных текстур
- ГенерироватьГрадиентЛинейный(width, height, direction, top, bottom) - линейный градиент (direction в градусах: 0=вертикальный, 90=горизонтальный)
- ГенерироватьГрадиентРадиальный(width, height, spread, inner, outer) - радиальный градиент
- ГенерироватьГрадиентКвадратный(width, height, direction, inner, outer) - квадратный градиент
- ГенерироватьШахматный(width, height, checksX, checksY, col1, col2) - шахматная доска
- ГенерироватьБелыйШум(width, height, factor) - белый шум (factor 0.0-1.0)
- ГенерироватьШумПерлина(width, height, offsetX, offsetY, scale) - шум Перлина
- ГенерироватьКлеточный(width, height, tileSize) - клеточный шум
- ЗагрузитьТекстуруИзИзображения(image) - загрузка текстуры из Image
- ВыгрузитьИзображение(image) - выгрузка изображения из памяти

Вспомогательные классы
- НовыйПрямоугольник(x, y, width, height) - создание прямоугольника
